# ********RoostGPT********

# Test generated by RoostGPT for test java-customannotation-test using AI Type  and AI Model 
# 
# Test generated for /me_get for http method type GET in artillery framework
# RoostTestHash=ae6b8ce5ab
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 5
  payload:
    path: "me_get_success.csv"
    fields:
      - "id"
      - "email"
    skipHeader: true
    delimiter: "^|^"
  environments:
    staging:
      target: "https://staging.example.com"
    production:
      target: "https://api.example.com"
  plugins:
    expect: {}

scenarios:
  - name: "Validate successful responses for /me endpoint"
    flow:
      - log: "Starting GET request for /me endpoint"
      - get:
          url: "/me?id={{ id }}"
          headers:
            Authorization: "Bearer {{$processEnvironment.AUTH_TOKEN}}"
            Content-Type: "application/json"
          expect:
            - statusCode: 200
            - hasProperty: "id"
            - hasProperty: "email"
            - hasProperty: "createdAt"
            - hasProperty: "did"
            - hasProperty: "name"
            - hasProperty: "image"
            - hasProperty: "settings"
            - hasProperty: "updatedAt"
            - contentType: "application/json"
            - headerEquals: { api-version: "1.0.0" }
      - think: 1
      - log: "Starting POST request to update /me endpoint"
      - post:
          url: "/me"
          headers:
            Authorization: "Bearer {{$processEnvironment.AUTH_TOKEN}}"
            Content-Type: "application/json"
          json:
            id: "{{ id }}"
            email: "{{ email }}"
            name: "Updated Name"
            settings: { theme: "dark" }
          expect:
            - statusCode: 200
            - hasProperty: "id"
            - hasProperty: "email"
            - hasProperty: "createdAt"
            - hasProperty: "did"
            - hasProperty: "name"
            - hasProperty: "image"
            - hasProperty: "settings"
            - hasProperty: "updatedAt"
            - contentType: "application/json"
      - think: 1

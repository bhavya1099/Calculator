# ********RoostGPT********

# Test generated by RoostGPT for test java-customannotation-test using AI Type  and AI Model 
# 
# Test generated for /credentials_get for http method type GET in artillery framework
# RoostTestHash=66f52a572f
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 10
  payload:
    path: "credentials_get_success.csv"
    fields:
      - size
      - offset
      - id
    skipHeader: true
    delimiter: "^|^"
  environments:
    staging:
      target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}

scenarios:
  - name: "Validate Success Responses for Credentials API"
    flow:
      - log: "Starting GET request for /credentials with valid parameters"
      - get:
          url: "/credentials?size={{ size }}&offset={{ offset }}"
          headers:
            Authorization: "Bearer {{$processEnvironment.AUTH_TOKEN}}"
          expect:
            - statusCode: 200
            - hasProperty: "credentials"
            - hasProperty: "total"
            - headerEquals: {api-version: "1.0.0"}
            - headerEquals: {page-size: "{{ size }}"}
            - headerEquals: {page-offset: "{{ offset }}"}
      - think: 1
      - log: "Starting PATCH request for /credentials/{id} to update status"
      - patch:
          url: "/credentials/{{ id }}"
          headers:
            Authorization: "Bearer {{$processEnvironment.AUTH_TOKEN}}"
          json:
            status: "active"
          expect:
            - statusCode: 200
            - hasProperty: "id"
            - hasProperty: "status"
            - matchesRegexp: {status: "active"}
      - think: 1
      - log: "Starting DELETE request for /credentials/{id}"
      - delete:
          url: "/credentials/{{ id }}"
          headers:
            Authorization: "Bearer {{$processEnvironment.AUTH_TOKEN}}"
          expect:
            - statusCode: 200
            - hasProperty: "message"
            - equals: {message: "Credential deleted successfully"}
      - think: 1

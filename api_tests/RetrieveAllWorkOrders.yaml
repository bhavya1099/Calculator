# ********RoostGPT********

# Test generated by RoostGPT for test java-customannotation-test using AI Type  and AI Model 
# 
# ROOST_METHOD_HASH=de965f9280
# ROOST_METHOD_SIG_HASH=2226510a78
# 
#    ########## Scenario ##########  
# 
# {
#   feature: 'Feature: Work Order Management API',
#   background: null,
#   rule: null,
#   scenario: {
#     title: 'Scenario: Retrieve all work orders',
#     steps: 'Given the API endpoint is "/workorders"\n' +
#       'When I send a GET request to retrieve all work orders\n' +
#       'Then the response status code should be 200\n' +
#       'And the response should contain a list of work orders',
#     examples: ''
#   }
# }
# 

# ********RoostGPT********
config:
  target: "https://pkc-00000.region.provider.confluent.cloud/kafka/v3/clusters"
  phases:
    - duration: 60
      arrivalRate: 1
  payload:
    path: "RetrieveAllWorkOrders.csv"
    fields:
      - "cluster_id"
    skipHeader: true
    delimiter: "^|^"
  environments:
    development:
      target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}

scenarios:
  - name: "Scenario: Retrieve all work orders"
    flow:
      - log: "Sending GET request to retrieve all work orders."
      - get:
          url: "/{{ cluster_id }}/topics"
          headers:
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
      - think: 2
      - log: "Expecting a 200 response with a list of work orders."
      - expect:
          statusCode: 200
          hasProperty: "data"

# ********RoostGPT********

# Test generated by RoostGPT for test java-customannotation-test using AI Type  and AI Model 
# 
# Test generated for /kafka/v3/clusters/{cluster_id}/topics_post for http method type POST in artillery framework
# RoostTestHash=783e9d2ab5
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.yuio_API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 1
  payload:
    path: "kafka_v3_clusters_cluster_id_topics_post_others.csv"
    skipHeader: true
    delimiter: "^|^"
  plugins:
    expect: {}

scenarios:
  - name: "Test Non-Success API Responses for Topic Creation"
    flow:
      - log: "Sending request with invalid auth token."
      - post:
          url: "/kafka/v3/clusters/cluster-1/topics"
          headers:
            Authorization: "Bearer {{$processEnvironment.yuio_AUTH_TOKEN}}"
          json:
            topic_name: "topic-invalid-auth"
            partitions_count: 1
            replication_factor: 1
            configs: []
          expect:
            - statusCode: 401
      - think: 1

      - log: "Sending request with incomplete payload (missing required 'topic_name')."
      - post:
          url: "/kafka/v3/clusters/cluster-1/topics"
          headers:
            Authorization: "Bearer {{$processEnvironment.yuio_AUTH_TOKEN}}"
          json:
            partitions_count: 2
            replication_factor: 1
            configs: []
          expect:
            - statusCode: 400
      - think: 1
      
      - log: "Sending request to an invalid URL."
      - post:
          url: "/kafka/v3/clusters/cluster-1/wrongtopics"
          headers:
            Authorization: "Bearer {{$processEnvironment.yuio_AUTH_TOKEN}}"
          json:
            topic_name: "valid-topic"
            partitions_count: 2
            replication_factor: 2
            configs: []
          expect:
            - statusCode: 404
      - think: 1

      - log: "Sending request with too high replication factor."
      - post:
          url: "/kafka/v3/clusters/cluster-1/topics"
          headers:
            Authorization: "Bearer {{$processEnvironment.yuio_AUTH_TOKEN}}"
          json:
            topic_name: "high-replication"
            partitions_count: 1
            replication_factor: 10000  # Assuming this is too high.
            configs: []
          expect:
            - statusCode: 422
      - think: 1

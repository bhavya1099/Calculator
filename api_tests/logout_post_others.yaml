# ********RoostGPT********

# Test generated by RoostGPT for test java-customannotation-test using AI Type  and AI Model 
# 
# Test generated for /logout_post for http method type POST in artillery framework
# RoostTestHash=64d33906e5
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 1
  payload:
    path: "logout_post_others.csv"
    fields:
      - "invalidApiKey"
      - "invalidPayloadData"
    skipHeader: true
    delimiter: "^|^"
  plugins:
    expect: {}

scenarios:
  - name: "API Non-Success Test Scenarios"
    flow:
      - log: "Testing with invalid API key"
      - post:
          url: "/logout"
          headers:
            ApiKey: "{{ $processEnvironment.AUTH_TOKEN }}INVALID"
          json:
            session: "{{ invalidPayloadData }}"
          expect:
            - statusCode: 401
            - hasProperty: "error"
      - think: 1

      - log: "Testing with invalid payload"
      - post:
          url: "/logout"
          headers:
            ApiKey: "{{ $processEnvironment.AUTH_TOKEN }}"
          json:
            sessionData: "{{ invalidPayloadData }}"
          expect:
            - statusCode: 400
            - hasProperty: "error"
      - think: 1

      - log: "Testing with missing authentication header"
      - post:
          url: "/logout"
          json:
            session: "{{ invalidPayloadData }}"
          expect:
            - statusCode: 403
            - hasProperty: "error"
      - think: 1

      - log: "Testing with incorrect URL"
      - post:
          url: "/loguot"
          headers:
            ApiKey: "{{ $processEnvironment.AUTH_TOKEN }}"
          json:
            session: "{{ invalidPayloadData }}"
          expect:
            - statusCode: 404
            - hasProperty: "error"
      - think: 1

# ********RoostGPT********

# Test generated by RoostGPT for test java-customannotation-test using AI Type  and AI Model 
# 
# Test generated for /credentials/{id}_get for http method type GET in artillery framework
# RoostTestHash=018c225aea
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 1
  payload:
    path: "credentials_id_get_others.csv"
    fields:
      - "id"
    skipHeader: true
    delimiter: "^|^"
  plugins:
    expect: {}

scenarios:
  - name: "Test Non-Success API Responses"
    flow:
      - log: "Testing with invalid UUID in path parameter."
      - get:
          url: "/credentials/invalid-uuid"
          headers:
            Authorization: "Bearer {{$processEnvironment.AUTH_TOKEN}}"
          expect:
            - statusCode: 400
      - think: 1

      - log: "Testing with missing UUID in path parameter."
      - get:
          url: "/credentials/"
          headers:
            Authorization: "Bearer {{$processEnvironment.AUTH_TOKEN}}"
          expect:
            - statusCode: 404
      - think: 1

      - log: "Testing with unauthorized access."
      - get:
          url: "/credentials/{{ id }}"
          headers:
            Authorization: "Bearer wrong-token"
          expect:
            - statusCode: 401
      - think: 1

      - log: "Testing with incorrect URL."
      - get:
          url: "/credential/{{ id }}"
          headers:
            Authorization: "Bearer {{$processEnvironment.AUTH_TOKEN}}"
          expect:
            - statusCode: 404
      - think: 1

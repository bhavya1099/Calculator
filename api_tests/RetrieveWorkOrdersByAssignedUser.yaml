# ********RoostGPT********

# Test generated by RoostGPT for test java-customannotation-test using AI Type  and AI Model 
# 
# ROOST_METHOD_HASH=82a3ebe016
# ROOST_METHOD_SIG_HASH=387615e444
# 
#    ########## Scenario ##########  
# 
# {
#   feature: 'Feature: Work Order Management API',
#   background: null,
#   rule: null,
#   scenario: {
#     title: 'Scenario: Retrieve work orders by assigned user',
#     steps: 'Given the API endpoint is "/workorders?assignedTo=John Doe"\n' +
#       'When I send a GET request to retrieve work orders assigned to "John Doe"\n' +
#       'Then the response status code should be 200\n' +
#       'And the response should contain a list of work orders assigned to "John Doe"',
#     examples: ''
#   }
# }
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 1
  environments:
    dev:
      API_HOST: "https://pkc-00000.region.provider.confluent.cloud"
      AUTH_TOKEN: "Bearer yourAuthToken"
  payload:
    path: "RetrieveWorkOrdersByAssignedUser.csv"
    fields:
      - "assignedTo"
    skipHeader: true
    delimiter: "^|^"
  plugins:
    expect: {}

scenarios:
  - name: "Retrieve work orders by assigned user"
    flow:
      - get:
          url: "/workorders?assignedTo={{ assignedTo }}"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          log: "Sending GET request to retrieve work orders assigned to John Doe"
          expect:
            - statusCode: 200
            - hasProperty: "workOrders"
          capture:
            json: "$.workOrders"
            as: "retrievedWorkOrders"
      - log: "Expecting the response to contain a list of work orders assigned to John Doe"
      - think: 1

# ********RoostGPT********

# Test generated by RoostGPT for test java-customannotation-test using AI Type  and AI Model 
# 
# ROOST_METHOD_HASH=f53f0e46c1
# ROOST_METHOD_SIG_HASH=bd730d3207
# 
#    ########## Scenario ##########  
# 
# {
#   feature: 'Feature: Work Order Management API',
#   background: null,
#   rule: null,
#   scenario: {
#     title: 'Scenario: Delete an existing work order',
#     steps: 'Given the API endpoint is "/workorders/{id}"\n' +
#       'And the work order ID is "WO123"\n' +
#       'When I send a DELETE request to delete the work order\n' +
#       'Then the response status code should be 204',
#     examples: ''
#   }
# }
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 1
  environments:
    dev:
      API_HOST: "https://pkc-00000.region.provider.confluent.cloud"
  payload:
    path: "DeleteExistingWorkOrder.csv"
    skipHeader: true
    fields:
      - "id"
    delimiter: "^|^"
  plugins:
    expect: {}

scenarios:
  - name: "Delete an existing work order"
    flow:
      - log: "Sending DELETE request to delete the work order with ID {{ id }}"
      - delete:
          url: "/kafka/v3/clusters/{cluster_id}/topics/{topic_name}/records"
          headers:
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          json:
            partition_id: 0
            headers: 
              - name: "string"
                value: "string"
            key:
              type: "string"
              data: null
            value:
              type: "string"
              data: null
            timestamp: "2019-08-24T14:15:22Z"
          expect:
            - statusCode: 204
      - think: 1

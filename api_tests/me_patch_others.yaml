# ********RoostGPT********

# Test generated by RoostGPT for test java-customannotation-test using AI Type  and AI Model 
# 
# Test generated for /me_patch for http method type PATCH in artillery framework
# RoostTestHash=6dd436357d
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 1
  payload:
    path: "me_patch_others.csv"
    fields:
      - name
      - unauthorizedField
      - Api-Key
    skipHeader: true
    delimiter: "^|^"
  environments:
    staging:
      target: "https://staging.example.com"
    production:
      target: "https://api.example.com"
  plugins:
    expect: {}

scenarios:
  - name: "API Misuse Scenarios"
    flow:
      - log: "Testing with missing required fields"
      - post:
          url: "/me"
          json:
            name: ""
          headers:
            Content-Type: "application/json"
            Api-Key: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 400
      - think: 1

      - log: "Testing with invalid authentication"
      - post:
          url: "/me"
          json:
            name: "John Doe"
          headers:
            Content-Type: "application/json"
            Api-Key: "InvalidToken"
          expect:
            - statusCode: 401
      - think: 1

      - log: "Testing with invalid URL"
      - post:
          url: "/invalidEndpoint"
          json:
            name: "John Doe"
          headers:
            Content-Type: "application/json"
            Api-Key: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 404
      - think: 1

      - log: "Testing with invalid payload data type"
      - post:
          url: "/me"
          json:
            name: 12345  # Invalid data type for 'name'
          headers:
            Content-Type: "application/json"
            Api-Key: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 422
      - think: 1

      - log: "Testing with extra non-defined field"
      - post:
          url: "/me"
          json:
            name: "John Doe"
            unauthorizedField: "data"
          headers:
            Content-Type: "application/json"
            Api-Key: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 400
      - think: 1

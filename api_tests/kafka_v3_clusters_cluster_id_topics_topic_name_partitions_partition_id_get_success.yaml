# ********RoostGPT********

# Test generated by RoostGPT for test java-customannotation-test using AI Type  and AI Model 
# 
# Test generated for /kafka/v3/clusters/{cluster_id}/topics/{topic_name}/partitions/{partition_id}_get for http method type GET in artillery framework
# RoostTestHash=0ecd20dc17
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.IASn_API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 5
  payload:
    path: "kafka_v3_clusters_cluster_id_topics_topic_name_partitions_partition_id_get_success.csv"
    fields:
      - "cluster_id"
      - "topic_name"
      - "partition_id"
    skipHeader: true
    delimiter: "^|^"
  plugins:
    expect: {}

scenarios:
  - name: "Validate successful responses for Kafka Partition API"
    flow:
      - log: "Starting GET request for Kafka Partition"
      - get:
          url: "/kafka/v3/clusters/{{ cluster_id }}/topics/{{ topic_name }}/partitions/{{ partition_id }}"
          headers:
            Authorization: "Bearer {{ $processEnvironment.IASn_AUTH_TOKEN }}"
          expect:
            - statusCode: 200
            - hasProperty: "kind"
            - hasProperty: "metadata"
            - hasProperty: "cluster_id"
            - hasProperty: "topic_name"
            - hasProperty: "partition_id"
            - hasProperty: "leader"
            - hasProperty: "replicas"
            - hasProperty: "reassignment"
      - think: 1
      - log: "Starting GET request for Kafka Partition with different parameters"
      - get:
          url: "/kafka/v3/clusters/{{ cluster_id }}/topics/{{ topic_name }}/partitions/{{ partition_id }}"
          headers:
            Authorization: "Bearer {{ $processEnvironment.IASn_AUTH_TOKEN }}"
          expect:
            - statusCode: 200
            - hasProperty: "kind"
            - hasProperty: "metadata"
            - hasProperty: "cluster_id"
            - hasProperty: "topic_name"
            - hasProperty: "partition_id"
            - hasProperty: "leader"
            - hasProperty: "replicas"
            - hasProperty: "reassignment"
      - think: 1

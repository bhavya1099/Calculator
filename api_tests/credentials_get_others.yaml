# ********RoostGPT********

# Test generated by RoostGPT for test java-customannotation-test using AI Type  and AI Model 
# 
# Test generated for /credentials_get for http method type GET in artillery framework
# RoostTestHash=66f52a572f
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 1
  environments:
    production:
      target: "https://api.production.com"
  payload:
    path: "credentials_get_others.csv"
    skipHeader: true
    delimiter: "^|^"
    fields:
      - "invalidApiKey"
      - "invalidPayload"
  plugins:
    expect: {}

scenarios:
  - name: "Test non-successful API responses"
    flow:
      - log: "Using invalid API key to test authentication failure."
      - post:
          url: "/credentials?size={{ size }}"
          headers:
            ApiKey: "{{ $processEnvironment.AUTH_TOKEN }}"
          json:
            size: 999
          expect:
            - statusCode: 401
            - hasProperty: "error"
      - think: 1

      - log: "Sending an oversized payload to test payload validation."
      - post:
          url: "/credentials"
          headers:
            ApiKey: "{{ $processEnvironment.AUTH_TOKEN }}"
          json:
            size: 100
          expect:
            - statusCode: 413
            - hasProperty: "message"
      - think: 1

      - log: "Using an unsupported method to test method validation on the server."
      - get:
          url: "/credentials?size=1"
          headers:
            ApiKey: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 405
            - hasProperty: "error"
      - think: 1

      - log: "Testing with invalid URL to test endpoint correctness."
      - post:
          url: "/invalidEndpoint"
          headers:
            ApiKey: "{{ $processEnvironment.AUTH_TOKEN }}"
          json:
            size: 10
          expect:
            - statusCode: 404
            - hasProperty: "error"
      - think: 1

      - log: "Sending request without required query parameters to test parameter validation."
      - get:
          url: "/credentials"
          headers:
            ApiKey: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 400
            - hasProperty: "error"
      - think: 1

# ********RoostGPT********

# Test generated by RoostGPT for test java-customannotation-test using AI Type  and AI Model 
# 
# Test generated for /kafka/v3/clusters/{cluster_id}/topics_get for http method type GET in artillery framework
# RoostTestHash=5a89adf6e3
# 
# 

# ********RoostGPT********
config:
  target: "https://pkc-00000.region.provider.confluent.cloud"
  phases:
    - duration: 60
      arrivalRate: 1
  payload:
    path: "kafka_v3_clusters_cluster_id_topics_get_others.csv"
    skipHeader: true
    fields:
      - "cluster_id"
      - "invalid_payload"
      - "invalid_token"
    delimiter: "^|^"
  environments:
    production:
      API_HOST: "https://pkc-00000.region.provider.confluent.cloud"
      AUTH_TOKEN: "Basic REPLACE_BASIC_AUTH"
  plugins:
    expect: {}

scenarios:
  - name: "Non-Success API Response Scenarios"
    flow:
      - log: "Testing with invalid cluster_id format"

      - get:
          url: "/kafka/v3/clusters/{{ cluster_id }}/topics"
          headers:
            Authorization: "{{ $processEnvironment.yuio_AUTH_TOKEN }}"
          capture:
            json: "$.id"
            as: "response_id"
          expect:
            - statusCode: 400
            - hasProperty: 'title'
            - hasProperty: 'detail'
      - think: 2

      - log: "Testing with incorrect authorization token"
      - get:
          url: "/kafka/v3/clusters/{{ cluster_id }}/topics"
          headers:
            Authorization: "{{ invalid_token }}"
          expect:
            - statusCode: 401
            - hasProperty: 'title'
            - hasProperty: 'detail'
      - think: 2

      - log: "Testing with missing required fields in payload"
      - post:
          url: "/kafka/v3/clusters/{{ cluster_id }}/topics"
          headers:
            Authorization: "{{ $processEnvironment.yuio_AUTH_TOKEN }}"
          json:
            something: "{{ invalid_payload }}"
          expect:
            - statusCode: 422
            - hasProperty: 'code'
            - hasProperty: 'message'
      - think: 2

      - log: "Testing with invalid URL"
      - get:
          url: "/kafka/v3/wrong/clusters/{{ cluster_id }}/topics"
          headers:
            Authorization: "{{ $processEnvironment.yuio_AUTH_TOKEN }}"
          expect:
            - statusCode: 404
            - hasProperty: 'title'
            - hasProperty: 'detail'
      - think: 2

# ********RoostGPT********

# Test generated by RoostGPT for test java-customannotation-test using AI Type  and AI Model 
# 
# Test generated for /kafka/v3/clusters/{cluster_id}/topics_get for http method type GET in artillery framework
# RoostTestHash=5a89adf6e3
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.ALIAS_API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 1
  payload:
    path: "kafka_v3_clusters_cluster_id_topics_get_others.csv"
    fields:
      - "cluster_id"
      - "invalid_token"
    skipHeader: true
    delimiter: "^|^"
  environments:
    development:
      API_HOST: "https://dev-api.example.com"
    production:
      API_HOST: "https://api.example.com"
  plugins:
    expect: {}

scenarios:
  - name: "Test Non-Success API Responses"
    flow:
      - log: "Testing with an invalid API key"
      - get:
          url: "/kafka/v3/clusters/{{ cluster_id }}/topics"
          headers:
            Authorization: "Bearer {{ invalid_token }}"
          expect:
            - statusCode: 401
      - think: 1

      - log: "Testing with missing required fields"
      - post:
          url: "/kafka/v3/clusters/{{ cluster_id }}/topics"
          json:
            name: ""
          headers:
            Authorization: "Bearer {{$processEnvironment.ALIAS_AUTH_TOKEN}}"
          expect:
            - statusCode: 400
            - hasProperty: "detail"
      - think: 1

      - log: "Testing with an invalid URL"
      - get:
          url: "/kafka/v3/clusters/{{ cluster_id }}/invalidEndpoint"
          headers:
            Authorization: "Bearer {{$processEnvironment.ALIAS_AUTH_TOKEN}}"
          expect:
            - statusCode: 404
      - think: 1

      - log: "Testing with malformed JSON body"
      - post:
          url: "/kafka/v3/clusters/{{ cluster_id }}/topics"
          body: "{'malformedJson': true"
          headers:
            Authorization: "Bearer {{$processEnvironment.ALIAS_AUTH_TOKEN}}"
            Content-Type: "application/json"
          expect:
            - statusCode: 400
      - think: 1

      - log: "Testing with an expired token"
      - get:
          url: "/kafka/v3/clusters/{{ cluster_id }}/topics"
          headers:
            Authorization: "Bearer expired_token_123"
          expect:
            - statusCode: 403
      - think: 1

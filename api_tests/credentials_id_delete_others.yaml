# ********RoostGPT********

# Test generated by RoostGPT for test java-customannotation-test using AI Type  and AI Model 
# 
# Test generated for /credentials/{id}_delete for http method type DELETE in artillery framework
# RoostTestHash=f1fc1d06f7
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 1
  payload:
    path: "credentials_id_delete_others.csv"
    fields:
      - "id"
    skipHeader: true
    delimiter: "^|^"
  plugins:
    expect: {}

scenarios:
  - name: "Test Non-Success API Responses"
    flow:
      - log: "Testing with invalid ID format"
      - post:
          url: "/credentials/invalid-id-format"
          headers:
            Content-Type: "application/json"
            ApiKey: "{{ $processEnvironment.AUTH_TOKEN }}"
          json:
            id: "1234"
          expect:
            - statusCode: 400
      - think: 2

      - log: "Testing with missing ID in URL"
      - delete:
          url: "/credentials/"
          headers:
            Content-Type: "application/json"
            ApiKey: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 404
      - think: 2

      - log: "Testing with unauthorized access"
      - delete:
          url: "/credentials/{{ id }}"
          headers:
            Content-Type: "application/json"
          expect:
            - statusCode: 401
      - think: 2

      - log: "Testing with valid ID but no API key"
      - delete:
          url: "/credentials/{{ id }}"
          headers:
            Content-Type: "application/json"
          expect:
            - statusCode: 403
      - think: 2

      - log: "Testing with non-existent ID"
      - delete:
          url: "/credentials/non-existent-id"
          headers:
            Content-Type: "application/json"
            ApiKey: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 404
      - think: 2

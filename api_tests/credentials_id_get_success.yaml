# ********RoostGPT********

# Test generated by RoostGPT for test java-customannotation-test using AI Type  and AI Model 
# 
# Test generated for /credentials/{id}_get for http method type GET in artillery framework
# RoostTestHash=018c225aea
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 10
  payload:
    path: "credentials_id_get_success.csv"
    fields:
      - "id"
    skipHeader: true
    delimiter: "^|^"
  plugins:
    expect: {}

scenarios:
  - name: "Validate successful responses for credential retrieval"
    flow:
      - log: "Starting GET request for credential retrieval"
      - get:
          url: "/credentials/{{ id }}"
          headers:
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          capture:
            json: "$.data"
            as: "response_data"
          expect:
            - statusCode: 200
            - hasProperty: "id"
            - hasProperty: "format"
            - hasProperty: "credential"
            - hasProperty: "status"
            - hasProperty: "statusUpdatedAt"
            - hasProperty: "createdAt"
            - matchesRegexp: "id"
            - matchesRegexp: "format"
            - matchesRegexp: "credential"
            - matchesRegexp: "status"
            - matchesRegexp: "statusUpdatedAt"
            - matchesRegexp: "createdAt"
      - think: 1

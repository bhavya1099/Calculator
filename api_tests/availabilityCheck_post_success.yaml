# ********RoostGPT********

# Test generated by RoostGPT for test karate-test using AI Type Open AI and AI Model gpt-4-turbo
# 
# Test generated for /availabilityCheck_post for http method type POST in artillery framework
# RoostTestHash=bc409fdb92
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 10
  payload:
    path: "availabilityCheck_post_success.csv"
    fields:
      - "subscriberId"
      - "addressId"
      - "complementType"
      - "complementDescription"
      - "complementValue"
    skipHeader: true
    delimiter: "^|^"
  plugins:
    expect: {}

scenarios:
  - name: "Validate successful responses for availability check API"
    flow:
      - log: "Starting POST request to /availabilityCheck with valid subscriber and address"
      - post:
          url: "/availabilityCheck"
          headers:
            Content-Type: "application/json"
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          json:
            customer:
              subscriberId: "{{ subscriberId }}"
            address:
              id: "{{ addressId }}"
              complement:
                complements:
                  - type: "{{ complementType }}"
                    description: "{{ complementDescription }}"
                    value: "{{ complementValue }}"
          expect:
            - statusCode: 200
            - hasProperty: "control"
            - hasProperty: "resource"
            - hasProperty: "resource.inventoryId"
            - hasProperty: "resource.maxBandwidth"
            - hasProperty: "resource.availabilityType"
            - hasProperty: "resource.availabilityDescription"
            - hasProperty: "resource.products"
      - think: 1
      - log: "Starting another POST request to /availabilityCheck with different valid inputs"
      - post:
          url: "/availabilityCheck"
          headers:
            Content-Type: "application/json"
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          json:
            customer:
              subscriberId: "DIFF001"
            address:
              id: "12345"
              complement:
                complements:
                  - type: "BL"
                    description: "Block level"
                    value: "100"
          expect:
            - statusCode: 200
            - hasProperty: "control.type"
            - hasProperty: "control.message"
            - hasProperty: "control.code"
            - hasProperty: "resource.inventoryId"
            - hasProperty: "resource.products"
            - hasProperty: "resource.products.product[0].catalogId"
      - think: 1

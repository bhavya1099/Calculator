# ********RoostGPT********

# Test generated by RoostGPT for test karate-test using AI Type Open AI and AI Model gpt-4-turbo
# 
# Test generated for /availabilityCheck_post for http method type POST in artillery framework
# RoostTestHash=bc409fdb92
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 10
  payload:
    path: "availabilityCheck_post_success.csv"
    fields:
      - "subscriberId"
      - "addressId"
      - "complementType"
      - "complementDescription"
      - "complementValue"
    skipHeader: true
    delimiter: "^|^"
  environments:
    development:
      target: "http://dev.example.com"
    production:
      target: "http://example.com"
  plugins:
    expect: {}

scenarios:
  - name: "Validate successful availability check API responses"
    flow:
      - log: "Starting POST request for availability check with complements"
      - post:
          url: "/availabilityCheck"
          json:
            customer:
              subscriberId: "{{ subscriberId }}"
            address:
              id: "{{ addressId }}"
              complement:
                complements:
                  - type: "{{ complementType }}"
                    description: "{{ complementDescription }}"
                    value: "{{ complementValue }}"
          headers:
            Content-Type: "application/json"
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 200
            - hasProperty: "control.type"
            - hasProperty: "resource.inventoryId"
            - hasProperty: "resource.maxBandwidth"
            - hasProperty: "resource.availabilityType"
            - hasProperty: "resource.availabilityCode"
            - hasProperty: "resource.availabilityDescription"
            - hasProperty: "resource.products"
      - think: 1
      - log: "Starting POST request for availability check without complements"
      - post:
          url: "/availabilityCheck"
          json:
            customer:
              subscriberId: "{{ subscriberId }}"
            address:
              id: "{{ addressId }}"
          headers:
            Content-Type: "application/json"
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 200
            - hasProperty: "control.type"
            - hasProperty: "resource.availabilityType"
            - hasProperty: "resource.availabilityCode"
      - think: 1

# ********RoostGPT********

# Test generated by RoostGPT for test java-customannotation-test using AI Type  and AI Model 
# 
# Test generated for /kafka/v3/clusters/{cluster_id}/topics_get for http method type GET in artillery framework
# RoostTestHash=5a89adf6e3
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.yuio_API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 1
  payload:
    path: "kafka_v3_clusters_cluster_id_topics_get_success.csv"
    skipHeader: true
    delimiter: "^|^"
    fields:
      - "cluster_id"
  environments:
    env:
      API_HOST: "https://api.example.com"
      AUTH_TOKEN: "auth-token-value"
  plugins:
    expect: {}

scenarios:
  - name: "List Kafka Topics API Test"
    flow:
      - log: "Starting GET request to list Kafka topics"
      - get:
          url: "/kafka/v3/clusters/{{ cluster_id }}/topics"
          headers:
            Authorization: "Bearer {{$processEnvironment.yuio_AUTH_TOKEN}}"
            Content-Type: "application/json"
          expect:
            - statusCode: 200
      - think: 1
      - log: "Checking properties in response"
      - get:
          url: "/kafka/v3/clusters/{{ cluster_id }}/topics"
          headers:
            Authorization: "Bearer {{$processEnvironment.yuio_AUTH_TOKEN}}"
            Content-Type: "application/json"
          expect:
            - hasProperty: "kind"
      - get:
          url: "/kafka/v3/clusters/{{ cluster_id }}/topics"
          headers:
            Authorization: "Bearer {{$processEnvironment.yuio_AUTH_TOKEN}}"
            Content-Type: "application/json"
          expect:
            - hasProperty: "metadata"
      - get:
          url: "/kafka/v3/clusters/{{ cluster_id }}/topics"
          headers:
            Authorization: "Bearer {{$processEnvironment.yuio_AUTH_TOKEN}}"
            Content-Type: "application/json"
          expect:
            - hasProperty: "data"
      - get:
          url: "/kafka/v3/clusters/{{ cluster_id }}/topics"
          headers:
            Authorization: "Bearer {{$processEnvironment.yuio_AUTH_TOKEN}}"
            Content-Type: "application/json"
          expect:
            - hasHeader: "Content-Type"
      - get:
          url: "/kafka/v3/clusters/{{ cluster_id }}/topics"
          headers:
            Authorization: "Bearer {{$processEnvironment.yuio_AUTH_TOKEN}}"
            Content-Type: "application/json"
          expect:
            - headerEquals:
                name: "Content-Type"
                value: "application/json"

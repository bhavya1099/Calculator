# ********RoostGPT********

# Test generated by RoostGPT for test java-customannotation-test using AI Type  and AI Model 
# 
# ROOST_METHOD_HASH=f0c8dfdbb0
# ROOST_METHOD_SIG_HASH=d3b81bf0e1
# 
#    ########## Scenario ##########  
# 
# {
#   feature: 'Feature: Work Order Management API',
#   background: null,
#   rule: null,
#   scenario: {
#     title: 'Scenario: Retrieve work orders by priority',
#     steps: 'Given the API endpoint is "/workorders?priority=High"\n' +
#       'When I send a GET request to retrieve work orders with priority "High"\n' +
#       'Then the response status code should be 200\n' +
#       'And the response should contain a list of high priority work orders',
#     examples: ''
#   }
# }
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 1
  defaults:
    headers:
      Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
  payload:
    path: "RetrieveWorkOrdersByPriority.csv"
    fields:
      - "priority"
    skipHeader: true
    delimiter: "^|^"
  plugins:
    expect: {}

scenarios:
  - name: "Retrieve work orders by priority"
    flow:
      - get:
          url: "/workorders?priority={{ priority }}"
          log: "Sending GET request to retrieve work orders with priority High."
          expect:
            - statusCode: 200
            - hasProperty: "kind"
            - hasProperty: "metadata"
            - hasProperty: "data"
        log: "Expecting status code 200 and a list of high priority work orders."
      - think: 1

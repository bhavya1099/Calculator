# ********RoostGPT********

# Test generated by RoostGPT for test java-customannotation-test using AI Type  and AI Model 
# 
# Test generated for /kafka/v3/clusters/{cluster_id}/topics/{topic_name}/partitions/{partition_id}_get for http method type GET in artillery framework
# RoostTestHash=0ecd20dc17
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.IASn_API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 1
  environments:
    production:
      target: "https://pkc-00000.region.provider.confluent.cloud"
  payload:
    path: "kafka_v3_clusters_cluster_id_topics_topic_name_partitions_partition_id_get_others.csv"
    fields: ["cluster_id", "topic_name", "partition_id"]
    skipHeader: true
    delimiter: "^|^"
  plugins:
    expect: {}

scenarios:
  - name: "Test Non-Success API Responses"
    flow:
      - log: "Testing invalid cluster ID"
      - get:
          url: "/kafka/v3/clusters/{{ cluster_id }}/topics/topic-1/partitions/0"
          headers:
            Authorization: "Bearer {{$processEnvironment.IASn_AUTH_TOKEN}}"
          expect:
            - statusCode: 404
            - hasProperty: "error_code"
            - hasProperty: "message"
      - think: 1
      - log: "Testing invalid topic name"
      - get:
          url: "/kafka/v3/clusters/cluster-1/topics/{{ topic_name }}/partitions/0"
          headers:
            Authorization: "Bearer {{$processEnvironment.IASn_AUTH_TOKEN}}"
          expect:
            - statusCode: 404
            - hasProperty: "error_code"
            - hasProperty: "message"
      - think: 1
      - log: "Testing invalid partition ID"
      - get:
          url: "/kafka/v3/clusters/cluster-1/topics/topic-1/partitions/{{ partition_id }}"
          headers:
            Authorization: "Bearer {{$processEnvironment.IASn_AUTH_TOKEN}}"
          expect:
            - statusCode: 404
            - hasProperty: "error_code"
            - hasProperty: "message"
      - think: 1
      - log: "Testing with incorrect API URL"
      - get:
          url: "/kafka/v3/invalidEndpoint"
          headers:
            Authorization: "Bearer {{$processEnvironment.IASn_AUTH_TOKEN}}"
          expect:
            - statusCode: 404
            - hasProperty: "error_code"
            - hasProperty: "message"
      - think: 1

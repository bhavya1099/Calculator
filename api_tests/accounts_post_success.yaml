# ********RoostGPT********

# Test generated by RoostGPT for test java-customannotation-test using AI Type  and AI Model 
# 
# Test generated for /accounts_post for http method type POST in artillery framework
# RoostTestHash=53e96f9805
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 10
  payload:
    path: "accounts_post_success.csv"
    fields:
      - "email"
    skipHeader: true
    delimiter: "^|^"
  plugins:
    expect: {}

scenarios:
  - name: "Validate successful account creation"
    flow:
      - log: "Starting test for successful account creation"
      - post:
          url: "/accounts"
          headers:
            Content-Type: "application/json"
            commit-hash: "{{ commitHash }}"
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          json:
            email: "{{ email }}"
          expect:
            - statusCode: 201
            - hasHeader: "api-version"
            - headerEquals: 
                api-version: "1.0.0"
            - hasProperty: "message"
            - matchesRegexp: 
                message: "^Success"
      - think: 1

      - log: "Testing successful email submission for OTP"
      - post:
          url: "/accounts/email-otp"
          headers:
            Content-Type: "application/json"
            commit-hash: "{{ commitHash }}"
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          json:
            email: "{{ email }}"
          expect:
            - statusCode: 200
            - hasProperty: "otp"
            - matchesRegexp: 
                otp: "^[0-9]{6}$"
      - think: 1

      - log: "Verifying account status after OTP submission"
      - get:
          url: "/accounts/status"
          headers:
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 200
            - hasProperty: "status"
            - equals: 
                status: "active"
      - think: 1

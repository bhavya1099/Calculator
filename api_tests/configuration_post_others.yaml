# ********RoostGPT********

# Test generated by RoostGPT for test karate-test using AI Type Open AI and AI Model gpt-4-turbo
# 
# Test generated for /configuration_post for http method type POST in artillery framework
# RoostTestHash=f72d56dc59
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 1
  environments:
    production:
      target: "https://api.production.com"
  payload:
    path: "configuration_post_others.csv"
    fields:
      - "actionType"
      - "frequencyBand"
      - "wifiWPAKey"
      - "wifiIndex"
      - "SSID"
      - "passwd"
      - "broadcastSSID"
      - "wifiChannel"
      - "mode"
      - "subscriberId"
    skipHeader: true
    delimiter: "^|^"
  plugins:
    expect: {}

scenarios:
  - name: "Test Non-Success Scenarios for API"
    flow:
      - log: "Sending invalid action type to test error handling"
      - post:
          url: "/configuration"
          json:
            configuration:
              action:
                type: "{{ actionType }}"
                parameters:
                  frequencyBand: "{{ frequencyBand }}"
          headers:
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 400
            - hasProperty: "error"
      - think: 1

      - log: "Sending request with invalid authentication token"
      - post:
          url: "/configuration"
          json:
            configuration:
              action:
                type: "HGW_WIFI_ENABLE"
                parameters:
                  frequencyBand: "2.4GHz"
          headers:
            Authorization: "Bearer invalid_token"
          expect:
            - statusCode: 401
      - think: 1

      - log: "Sending valid request with non-existing subscriber ID"
      - post:
          url: "/configuration"
          json:
            configuration:
              action:
                type: "HGW_WIFI_ENABLE"
                parameters:
                  frequencyBand: "2.4GHz"
              customer:
                subscriberId: "NON_EXISTENT_ID"
          headers:
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 404
            - hasProperty: "error"
      - think: 1

      - log: "Sending request to an invalid URL"
      - post:
          url: "/invalidEndpoint"
          json:
            configuration:
              action:
                type: "HGW_WIFI_ENABLE"
                parameters:
                  frequencyBand: "2.4GHz"
          headers:
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 404
      - think: 1

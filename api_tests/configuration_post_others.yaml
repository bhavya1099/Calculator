# ********RoostGPT********

# Test generated by RoostGPT for test karate-test using AI Type Open AI and AI Model gpt-4-turbo
# 
# Test generated for /configuration_post for http method type POST in artillery framework
# RoostTestHash=f72d56dc59
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 1
  environments:
    production:
      target: "https://api.production.com"
  payload:
    path: "configuration_post_others.csv"
    fields:
      - "actionType"
      - "frequencyBand"
      - "wifiWPAKey"
      - "wifiIndex"
      - "SSID"
      - "passwd"
      - "broadcastSSID"
      - "wifiChannel"
      - "mode"
      - "subscriberId"
    skipHeader: true
    delimiter: "^|^"
  plugins:
    expect: {}

scenarios:
  - name: "Test Non-Success Scenarios for API"
    flow:
      - log: "Testing invalid action type which is not allowed"
      - post:
          url: "/configuration"
          json:
            configuration:
              action:
                type: "{{ actionType }}"
                parameters:
                  frequencyBand: "{{ frequencyBand }}"
          headers:
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 400
            - hasProperty: "diagnostic"
            - hasProperty: "diagnostic.code"
            - hasProperty: "diagnostic.messages"
      - think: 1

      - log: "Testing with unauthorized access"
      - post:
          url: "/configuration"
          json:
            configuration:
              action:
                type: "HGW_WIFI_ENABLE"
                parameters:
                  frequencyBand: "2.4GHz"
          expect:
            - statusCode: 401
      - think: 1

      - log: "Testing with missing required fields"
      - post:
          url: "/configuration"
          json:
            configuration:
              action:
                type: "HGW_WIFI_SET_PASSWD"
                parameters:
                  wifiIndex: "{{ wifiIndex }}"
          headers:
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 400
            - hasProperty: "diagnostic"
            - hasProperty: "diagnostic.code"
            - hasProperty: "diagnostic.messages"
      - think: 1

      - log: "Testing with invalid URL"
      - post:
          url: "/invalidUrl"
          json:
            configuration:
              action:
                type: "HGW_WIFI_DISABLE"
                parameters:
                  frequencyBand: "5GHz"
          headers:
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 404
      - think: 1

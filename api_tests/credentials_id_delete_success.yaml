# ********RoostGPT********

# Test generated by RoostGPT for test java-customannotation-test using AI Type  and AI Model 
# 
# Test generated for /credentials/{id}_delete for http method type DELETE in artillery framework
# RoostTestHash=f1fc1d06f7
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  environments:
    development:
      target: "http://dev.example.com"
    production:
      target: "http://example.com"
  phases:
    - duration: 60
      arrivalRate: 5
  payload:
    path: "credentials_id_delete_success.csv"
    fields:
      - "id"
    skipHeader: true
    delimiter: "^|^"
  plugins:
    expect: {}

scenarios:
  - name: "Delete Credential Success Scenario"
    flow:
      - log: "Starting DELETE request to /credentials/{id} with valid UUID"
      - delete:
          url: "/credentials/{{ id }}"
          headers:
            Api-Key: "{{ $processEnvironment.AUTH_token }}"
          expect:
            - statusCode: 200
            - hasHeader: "api-version"
            - headerEquals:
                api-version: "1.0.0"
      - think: 1

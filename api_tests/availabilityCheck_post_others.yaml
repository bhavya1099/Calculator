# ********RoostGPT********

# Test generated by RoostGPT for test karate-test using AI Type Open AI and AI Model gpt-4-turbo
# 
# Test generated for /availabilityCheck_post for http method type POST in artillery framework
# RoostTestHash=bc409fdb92
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 1
  payload:
    path: "availabilityCheck_post_others.csv"
    fields:
      - "subscriberId"
      - "addressId"
      - "type"
      - "description"
      - "value"
    skipHeader: true
    delimiter: "^|^"
  plugins:
    expect: {}

scenarios:
  - name: "Test various non-success API responses"
    flow:
      - log: "Sending a POST request with invalid subscriber ID length"
      - post:
          url: "/availabilityCheck"
          json:
            customer:
              subscriberId: "TOOLONGSUBSCRIBERIDEXCEEDSMAXLENGTH"
            address:
              id: "{{ addressId }}"
          headers:
            Content-Type: "application/json"
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 400
            - hasProperty: "error"
      - think: 2

      - log: "Sending a POST request with missing required fields"
      - post:
          url: "/availabilityCheck"
          json:
            customer: {}
          headers:
            Content-Type: "application/json"
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 400
            - hasProperty: "error"
      - think: 2

      - log: "Sending a POST request with invalid address ID type"
      - post:
          url: "/availabilityCheck"
          json:
            customer:
              subscriberId: "{{ subscriberId }}"
            address:
              id: "INVALIDID"
          headers:
            Content-Type: "application/json"
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 400
            - hasProperty: "error"
      - think: 2

      - log: "Sending a POST request to an incorrect URL"
      - post:
          url: "/invalidEndPoint"
          json:
            customer:
              subscriberId: "{{ subscriberId }}"
            address:
              id: "{{ addressId }}"
          headers:
            Content-Type: "application/json"
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 404
            - hasProperty: "error"
      - think: 2

# ********RoostGPT********

# Test generated by RoostGPT for test karate-test using AI Type Open AI and AI Model gpt-4-turbo
# 
# Test generated for /availabilityCheck_post for http method type POST in artillery framework
# RoostTestHash=bc409fdb92
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 1
  payload:
    path: "availabilityCheck_post_others.csv"
    fields:
      - "subscriberId"
      - "addressId"
      - "type"
      - "description"
      - "value"
    skipHeader: true
    delimiter: "^|^"
  plugins:
    expect: {}

scenarios:
  - name: "Test various non-success API responses"
    flow:
      - log: "Sending a request with invalid subscriberId format."
      - post:
          url: "/availabilityCheck"
          headers:
            Content-Type: "application/json"
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          json:
            customer:
              subscriberId: "INVALID_SUB_ID_1234567890123456"
            address:
              id: "{{ addressId }}"
              complement:
                complements: []
          expect:
            - statusCode: 400
            - hasProperty: "error"
      - think: 1

      - log: "Sending a request with missing required fields."
      - post:
          url: "/availabilityCheck"
          headers:
            Content-Type: "application/json"
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          json:
            customer: {}
            address:
              id: "{{ addressId }}"
          expect:
            - statusCode: 400
            - hasProperty: "error"
      - think: 1

      - log: "Sending a request with too many address complements."
      - post:
          url: "/availabilityCheck"
          headers:
            Content-Type: "application/json"
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          json:
            customer:
              subscriberId: "{{ subscriberId }}"
            address:
              id: "{{ addressId }}"
              complement:
                complements:
                  - type: "{{ type }}"
                    description: "{{ description }}"
                    value: "{{ value }}"
                  - type: "{{ type }}"
                    description: "{{ description }}"
                    value: "{{ value }}"
                  - type: "{{ type }}"
                    description: "{{ description }}"
                    value: "{{ value }}"
                  - type: "{{ type }}"
                    description: "{{ description }}"
                    value: "{{ value }}"
          expect:
            - statusCode: 400
            - hasProperty: "error"
      - think: 1

      - log: "Attempting to access API with incorrect URL."
      - post:
          url: "/invalidEndpoint"
          headers:
            Content-Type: "application/json"
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          json:
            customer:
              subscriberId: "{{ subscriberId }}"
            address:
              id: "{{ addressId }}"
          expect:
            - statusCode: 404
      - think: 1

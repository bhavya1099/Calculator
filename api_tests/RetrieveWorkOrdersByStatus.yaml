# ********RoostGPT********

# Test generated by RoostGPT for test java-customannotation-test using AI Type  and AI Model 
# 
# ROOST_METHOD_HASH=4f2d9d9489
# ROOST_METHOD_SIG_HASH=7a156baa44
# 
#    ########## Scenario ##########  
# 
# {
#   feature: 'Feature: Work Order Management API',
#   background: null,
#   rule: null,
#   scenario: {
#     title: 'Scenario: Retrieve work orders by status',
#     steps: 'Given the API endpoint is "/workorders?status=Open"\n' +
#       'When I send a GET request to retrieve work orders with status "Open"\n' +
#       'Then the response status code should be 200\n' +
#       'And the response should contain a list of open work orders',
#     examples: ''
#   }
# }
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 1
  environments:
    development:
      API_HOST: "https://pkc-00000.region.provider.confluent.cloud"
      AUTH_TOKEN: "Bearer your_development_auth_token"
    production:
      API_HOST: "https://pkc-00000.region.provider.confluent.cloud"
      AUTH_TOKEN: "Bearer your_production_auth_token"
  payload:
    path: "RetrieveWorkOrdersByStatus.csv"
    fields:
      - "status"
    skipHeader: true
    delimiter: "^|^"
  plugins:
    expect: {}

scenarios:
  - name: "Retrieve work orders by status"
    flow:
      - get:
          url: "/workorders?status={{ status }}"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 200
            - hasProperty: "open_work_orders"
      - log: "Request to retrieve open work orders sent."
      - think: 1
      - get:
          url: "/workorders?status=invalid_status"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 400
      - log: "Request with invalid status sent, expecting failure."
      - think: 1

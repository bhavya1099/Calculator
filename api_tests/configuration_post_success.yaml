# ********RoostGPT********

# Test generated by RoostGPT for test karate-test using AI Type Open AI and AI Model gpt-4-turbo
# 
# Test generated for /configuration_post for http method type POST in artillery framework
# RoostTestHash=f72d56dc59
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 10
  payload:
    path: "configuration_post_success.csv"
    fields:
      - "subscriberId"
      - "type"
      - "frequencyBand"
      - "wifiIndex"
      - "passwd"
      - "mode"
    skipHeader: true
    delimiter: "^|^"
  plugins:
    expect: {}

scenarios:
  - name: "Test Configuration API for various successful scenarios"
    flow:
      - log: "Starting API test for enabling WiFi"
      - post:
          url: "/configuration"
          headers:
            Content-Type: "application/json"
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          json:
            configuration:
              action:
                type: "{{ type }}"
                parameters:
                  frequencyBand: "{{ frequencyBand }}"
              customer:
                subscriberId: "{{ subscriberId }}"
          expect:
            - statusCode: 200
            - hasProperty: "configuration"
            - hasProperty: "control"
            - hasProperty: "control.type"
            - equals: 
                path: "control.message"
                value: "OK"
      - think: 1

      - log: "Starting API test for setting WiFi password"
      - post:
          url: "/configuration"
          headers:
            Content-Type: "application/json"
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          json:
            configuration:
              action:
                type: "{{ type }}"
                parameters:
                  wifiIndex: "{{ wifiIndex }}"
                  passwd: "{{ passwd }}"
                  mode: "{{ mode }}"
              customer:
                subscriberId: "{{ subscriberId }}"
          expect:
            - statusCode: 200
            - hasProperty: "configuration"
            - hasProperty: "control"
            - hasProperty: "control.code"
            - hasProperty: "control.type"
            - hasProperty: "control.message"
      - think: 1

      - log: "Starting API test for disabling WiFi"
      - post:
          url: "/configuration"
          headers:
            Content-Type: "application/json"
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          json:
            configuration:
              action:
                type: "{{ type }}"
                parameters:
                  frequencyBand: "{{ frequencyBand }}"
              customer:
                subscriberId: "{{ subscriberId }}"
          expect:
            - statusCode: 200
            - hasProperty: "configuration"
            - hasProperty: "control"
            - equals: 
                path: "control.type"
                value: "S"
      - think: 1

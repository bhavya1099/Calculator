# ********RoostGPT********

# Test generated by RoostGPT for test java-customannotation-test using AI Type  and AI Model 
# 
# Test generated for /logout_post for http method type POST in artillery framework
# RoostTestHash=64d33906e5
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 10
  payload:
    path: "logout_post_success.csv"
    fields:
      - "userId"
      - "sessionId"
    skipHeader: true
    delimiter: "^|^"
  plugins:
    expect: {}

scenarios:
  - name: "Logout API Success Test Scenario"
    flow:
      - log: "Starting test for logout with valid session"
      - post:
          url: "/logout"
          headers:
            Content-Type: "application/json"
            Authorization: "ApiKey {{ $processEnvironment.AUTH_TOKEN }}"
          json:
            userId: "{{ userId }}"
            sessionId: "{{ sessionId }}"
          expect:
            - statusCode: 200
            - hasHeader: "api-version"
            - headerEquals:
                name: "api-version"
                value: "1.0.0"
      - think: 1
      - log: "Re-testing logout for different valid session"
      - post:
          url: "/logout"
          headers:
            Content-Type: "application/json"
            Authorization: "ApiKey {{ $processEnvironment.AUTH_TOKEN }}"
          json:
            userId: "{{ userId }}"
            sessionId: "{{ sessionId }}"
          expect:
            - statusCode: 200
            - hasHeader: "api-version"
            - headerEquals:
                name: "api-version"
                value: "1.0.0"
      - think: 1

// ********RoostGPT********
/*
Test generated by RoostGPT for test demoTestGitlab using AI Type Open AI and AI Model gpt-4

1. Scenario: Test when the name is properly set.
   Description: In this scenario, we set a name value to the object, and then use the getName() function to retrieve it. The expected result should be the same as the value we set.

2. Scenario: Test when the name is not set.
   Description: In this scenario, we create an object without setting a name value. When we call the getName() function, it should return null or a default value, depending on the implementation of the class.

3. Scenario: Test when the name is set to an empty string.
   Description: Set the name as an empty string and call the getName() function. The returned result should be an empty string.

4. Scenario: Test when the name is set to a string with spaces.
   Description: Set the name as a string with spaces and call the getName() function. The returned result should be the same string with spaces.

5. Scenario: Test when the name is set to a long string.
   Description: Set the name as a long string (more than the expected or maximum limit) and call the getName() function. The returned result should be the same long string.

6. Scenario: Test when the name is set to special characters.
   Description: Set the name as a string of special characters and call the getName() function. The returned result should be the same string of special characters.

7. Scenario: Test when the name is set to non-English characters.
   Description: Set the name as a string of non-English characters (like Chinese, Arabic, etc.) and call the getName() function. The returned result should be the same string of non-English characters.
*/

// ********RoostGPT********
package com.houarizegai.calculator.theme.properties;

import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.*;

class Theme_getName_8400ac6fb7_Test {
    @Test
    void testGetNameWhenNameIsProperlySet() {
        Theme theme = new Theme();
        theme.setName("Dark");
        assertEquals("Dark", theme.getName());
    }

    @Test
    void testGetNameWhenNameIsNotSet() {
        Theme theme = new Theme();
        assertNull(theme.getName());
    }

    @Test
    void testGetNameWhenNameIsEmptyString() {
        Theme theme = new Theme();
        theme.setName("");
        assertEquals("", theme.getName());
    }

    @Test
    void testGetNameWhenNameIsStringWithSpaces() {
        Theme theme = new Theme();
        theme.setName("Dark Theme");
        assertEquals("Dark Theme", theme.getName());
    }

    @Test
    void testGetNameWhenNameIsLongString() {
        Theme theme = new Theme();
        theme.setName("This is a very very long string");
        assertEquals("This is a very very long string", theme.getName());
    }

    @Test
    void testGetNameWhenNameIsSpecialCharacters() {
        Theme theme = new Theme();
        theme.setName("@#$$%^&*()");
        assertEquals("@#$$%^&*()", theme.getName());
    }

    @Test
    void testGetNameWhenNameIsNonEnglishCharacters() {
        Theme theme = new Theme();
        theme.setName("テーマ");
        assertEquals("テーマ", theme.getName());
    }
}

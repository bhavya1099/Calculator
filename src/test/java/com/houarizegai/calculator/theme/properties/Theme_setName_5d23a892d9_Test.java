// ********RoostGPT********
/*
Test generated by RoostGPT for test demoTestGitlab using AI Type Open AI and AI Model gpt-4

1. Test Scenario: Check if the function accepts a valid string as a name.
   Test Steps: 
   - Instantiate the class that contains the setName method.
   - Call the setName method with a valid string as input.
   - Verify that the name is set correctly.

2. Test Scenario: Check if the function handles null input.
   Test Steps:
   - Instantiate the class that contains the setName method.
   - Call the setName method with null as input.
   - Verify that the name is set to null or the function handles the null input as expected.

3. Test Scenario: Check if the function handles empty string input.
   Test Steps:
   - Instantiate the class that contains the setName method.
   - Call the setName method with an empty string as input.
   - Verify that the name is set to an empty string or the function handles the empty string as expected.

4. Test Scenario: Check if the function handles long string input.
   Test Steps:
   - Instantiate the class that contains the setName method.
   - Call the setName method with a very long string as input.
   - Verify that the name is set to the long string or the function handles the long string as expected.

5. Test Scenario: Check if the function handles special character input.
   Test Steps:
   - Instantiate the class that contains the setName method.
   - Call the setName method with a string containing special characters as input.
   - Verify that the name is set to the string with special characters or the function handles the special characters as expected.

6. Test Scenario: Check if the function handles numeric input.
   Test Steps:
   - Instantiate the class that contains the setName method.
   - Call the setName method with a string containing numbers as input.
   - Verify that the name is set to the string with numbers or the function handles the numbers as expected.
*/

// ********RoostGPT********
package com.houarizegai.calculator.theme.properties;

import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.*;

public class Theme_setName_5d23a892d9_Test {

    @Test
    public void testSetName_ValidString() {
        Theme theme = new Theme();
        theme.setName("TestName");
        assertEquals("TestName", theme.getName());
    }

    @Test
    public void testSetName_NullInput() {
        Theme theme = new Theme();
        theme.setName(null);
        assertNull(theme.getName());
    }

    @Test
    public void testSetName_EmptyString() {
        Theme theme = new Theme();
        theme.setName("");
        assertEquals("", theme.getName());
    }

    @Test
    public void testSetName_LongString() {
        Theme theme = new Theme();
        String longString = "This is a very long string that is used for testing the setName method in the Theme class.";
        theme.setName(longString);
        assertEquals(longString, theme.getName());
    }

    @Test
    public void testSetName_SpecialCharacters() {
        Theme theme = new Theme();
        String specialCharacters = "!@#$%^&*()_+";
        theme.setName(specialCharacters);
        assertEquals(specialCharacters, theme.getName());
    }

    @Test
    public void testSetName_NumericInput() {
        Theme theme = new Theme();
        String numericInput = "1234567890";
        theme.setName(numericInput);
        assertEquals(numericInput, theme.getName());
    }
}

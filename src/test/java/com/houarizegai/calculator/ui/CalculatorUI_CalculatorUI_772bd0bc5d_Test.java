// ********RoostGPT********
/*
Test generated by RoostGPT for test demoTestGitlab using AI Type Open AI and AI Model gpt-4

Test Scenario 1: Test to check if the application window is set up correctly.
- Initialize a CalculatorUI instance.
- Check if the window is not null.
- Check if the window title is the same as APPLICATION_TITLE.
- Check if the window size is the same as WINDOW_WIDTH and WINDOW_HEIGHT.
- Check if the window location is set to null.
- Check if the window layout is null.
- Check if the window is not resizable.
- Check if the default close operation is JFrame.EXIT_ON_CLOSE.
- Check if the window is visible.

Test Scenario 2: Test to check if the themesMap is loaded correctly.
- Initialize a CalculatorUI instance.
- Check if the themesMap is not null.

Test Scenario 3: Test to check if the columns and rows are initialized correctly.
- Initialize a CalculatorUI instance.
- Check if the columns and rows are not null.
- Check if the length of columns array is 5.
- Check if the length of rows array is 6.
- Check if the values in the columns array are as expected (MARGIN_X, MARGIN_X + 90, MARGIN_X + 90 * 2, MARGIN_X + 90 * 3, MARGIN_X + 90 * 4).
- Check if the values in the rows array are as expected (MARGIN_Y, MARGIN_Y + 100, MARGIN_Y + 100 + 80, MARGIN_Y + 100 + 80 * 2, MARGIN_Y + 100 + 80 * 3, MARGIN_Y + 100 + 80 * 4).
*/

// ********RoostGPT********
import com.houarizegai.calculator.ui.CalculatorUI;
import org.junit.jupiter.api.BeforeEach;
import org.junit.jupiter.api.Test;

import java.lang.reflect.Field;

import static org.junit.jupiter.api.Assertions.*;

public class CalculatorUI_CalculatorUI_772bd0bc5d_Test {
    private CalculatorUI calculatorUI;

    @BeforeEach
    public void setUp() {
        calculatorUI = new CalculatorUI();
    }

    @Test
    public void testCalculatorUI() throws NoSuchFieldException, IllegalAccessException {
        assertNotNull(calculatorUI);

        // Test window setup
        Field windowField = CalculatorUI.class.getDeclaredField("window");
        windowField.setAccessible(true);
        JFrame window = (JFrame) windowField.get(calculatorUI);

        assertNotNull(window);
        assertEquals("Calculator", window.getTitle());
        assertEquals(410, window.getWidth());
        assertEquals(600, window.getHeight());
        assertNull(window.getLocation());
        assertNull(window.getLayout());
        assertFalse(window.isResizable());
        assertEquals(JFrame.EXIT_ON_CLOSE, window.getDefaultCloseOperation());
        assertTrue(window.isVisible());

        // Test themesMap
        Field themesMapField = CalculatorUI.class.getDeclaredField("themesMap");
        themesMapField.setAccessible(true);
        Map<String, Theme> themesMap = (Map<String, Theme>) themesMapField.get(calculatorUI);

        assertNotNull(themesMap);

        // Test columns and rows
        int[] columns = {20, 20 + 90, 20 + 90 * 2, 20 + 90 * 3, 20 + 90 * 4};
        int[] rows = {60, 60 + 100, 60 + 100 + 80, 60 + 100 + 80 * 2, 60 + 100 + 80 * 3, 60 + 100 + 80 * 4};

        assertNotNull(columns);
        assertEquals(5, columns.length);
        assertArrayEquals(new int[] {20, 20 + 90, 20 + 90 * 2, 20 + 90 * 3, 20 + 90 * 4}, columns);

        assertNotNull(rows);
        assertEquals(6, rows.length);
        assertArrayEquals(new int[] {60, 60 + 100, 60 + 100 + 80, 60 + 100 + 80 * 2, 60 + 100 + 80 * 3, 60 + 100 + 80 * 4}, rows);
    }
}

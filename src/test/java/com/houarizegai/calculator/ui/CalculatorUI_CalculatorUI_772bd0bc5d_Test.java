// ********RoostGPT********
/*
Test generated by RoostGPT for test demoTestGitlab using AI Type Open AI and AI Model gpt-4

1. Verify that the "CalculatorUI" function initializes the "themesMap" correctly by loading all themes.
2. Verify that the "CalculatorUI" function initializes a new JFrame with the title as "APPLICATION_TITLE".
3. Verify that the "CalculatorUI" function sets the JFrame size to "WINDOW_WIDTH" and "WINDOW_HEIGHT".
4. Check that the "CalculatorUI" function sets the JFrame location to the center of the screen.
5. Test that the "CalculatorUI" function initializes the "columns" and "rows" arrays correctly.
6. Verify that the "CalculatorUI" function calls the "initInputScreen" method with the correct "columns" and "rows" parameters.
7. Verify that the "CalculatorUI" function calls the "initButtons" method with the correct "columns" and "rows" parameters.
8. Verify that the "CalculatorUI" function calls the "initCalculatorTypeSelector" method.
9. Verify that the "CalculatorUI" function calls the "initThemeSelector" method.
10. Verify that the "CalculatorUI" function sets the JFrame layout to "null".
11. Check that the "CalculatorUI" function sets the JFrame to be non-resizable.
12. Verify that the "CalculatorUI" function sets the JFrame default close operation to "EXIT_ON_CLOSE".
13. Verify that the "CalculatorUI" function sets the JFrame to be visible.
*/

// ********RoostGPT********
package com.houarizegai.calculator.ui;

import org.junit.jupiter.api.*;
import org.junit.jupiter.api.extension.ExtendWith;
import org.mockito.InjectMocks;
import org.mockito.Mock;
import org.mockito.junit.jupiter.MockitoExtension;
import com.houarizegai.calculator.theme.properties.Theme;
import java.util.HashMap;
import java.util.Map;
import javax.swing.JFrame;

import static org.junit.jupiter.api.Assertions.*;
import static org.mockito.Mockito.*;

@ExtendWith(MockitoExtension.class)
public class CalculatorUI_CalculatorUI_772bd0bc5d_Test {

    @Mock
    private JFrame window;

    @Mock
    private Map<String, Theme> themesMap;

    @InjectMocks
    private CalculatorUI calculatorUI;

    @BeforeEach
    public void setUp() {
        themesMap = new HashMap<>();
    }

    @Test
    public void testCalculatorUI() {
        calculatorUI = new CalculatorUI();

        verify(themesMap, times(1)).loadThemes();
        verify(window, times(1)).setSize(410, 600);
        verify(window, times(1)).setLocationRelativeTo(null);
        verify(window, times(1)).setLayout(null);
        verify(window, times(1)).setResizable(false);
        verify(window, times(1)).setDefaultCloseOperation(JFrame.EXIT_ON_CLOSE);
        verify(window, times(1)).setVisible(true);
    }
}

// ********RoostGPT********
/*
Test generated by RoostGPT for test demoTestGitlab using AI Type Open AI and AI Model gpt-4

Test Scenario 1: Test to check if the application window is set up correctly.
- Given the CalculatorUI is initialized.
- When the window is set up.
- Then check if the window title matches the APPLICATION_TITLE.
- And check if the window size matches the WINDOW_WIDTH and WINDOW_HEIGHT.
- And check if the window location is set to null (center of the screen).

Test Scenario 2: Test to check if the window layout is set to null.
- Given the CalculatorUI is initialized.
- When the window layout is set.
- Then check if the layout is set to null.

Test Scenario 3: Test to check if the window is not resizable.
- Given the CalculatorUI is initialized.
- When the window resizable property is set.
- Then check if the window is not resizable.

Test Scenario 4: Test to check if the window default close operation is set to EXIT_ON_CLOSE.
- Given the CalculatorUI is initialized.
- When the default close operation is set.
- Then check if the default close operation is set to EXIT_ON_CLOSE.

Test Scenario 5: Test to check if the window is visible.
- Given the CalculatorUI is initialized.
- When the window is set to visible.
- Then check if the window is visible.

Test Scenario 6: Test to check if themes are loaded correctly.
- Given the CalculatorUI is initialized.
- When the themes are loaded.
- Then check if themesMap is not null.
- And check if themesMap contains the expected themes.

Test Scenario 7: Test to check if the columns and rows are set up correctly.
- Given the CalculatorUI is initialized.
- When the columns and rows are set up.
- Then check if the columns and rows match the expected values.
*/

// ********RoostGPT********
package com.houarizegai.calculator.ui;

import com.houarizegai.calculator.theme.ThemeLoader;
import com.houarizegai.calculator.theme.properties.Theme;
import org.junit.jupiter.api.BeforeEach;
import org.junit.jupiter.api.Test;

import javax.swing.*;
import java.awt.*;
import java.util.Map;

import static org.junit.jupiter.api.Assertions.*;

public class CalculatorUI_CalculatorUI_772bd0bc5d_Test {

    private CalculatorUI calculatorUI;
    private Map<String, Theme> themesMap;

    @BeforeEach
    public void setUp() {
        calculatorUI = new CalculatorUI();
        themesMap = ThemeLoader.loadThemes();
    }

    @Test
    public void testWindowSetup() {
        JFrame window = calculatorUI.window;
        assertEquals("Calculator", window.getTitle());
        assertEquals(new Dimension(410, 600), window.getSize());
        assertNull(window.getLocation());
    }

    @Test
    public void testWindowLayout() {
        assertNull(calculatorUI.window.getLayout());
    }

    @Test
    public void testWindowResizable() {
        assertFalse(calculatorUI.window.isResizable());
    }

    @Test
    public void testWindowCloseOperation() {
        assertEquals(JFrame.EXIT_ON_CLOSE, calculatorUI.window.getDefaultCloseOperation());
    }

    @Test
    public void testWindowVisibility() {
        assertTrue(calculatorUI.window.isVisible());
    }

    @Test
    public void testThemesLoading() {
        assertNotNull(calculatorUI.themesMap);
        assertEquals(themesMap, calculatorUI.themesMap);
    }

    @Test
    public void testColumnsAndRowsSetup() {
        int[] expectedColumns = {20, 110, 200, 290, 380};
        int[] expectedRows = {60, 160, 240, 320, 400, 480};

        // Assuming getColumns() and getRows() methods are available in the CalculatorUI class
        // assertArrayEquals(expectedColumns, calculatorUI.getColumns());
        // assertArrayEquals(expectedRows, calculatorUI.getRows());
    }
}

// ********RoostGPT********
/*
Test generated by RoostGPT for test demoTestGitlab using AI Type Open AI and AI Model gpt-4

Test generated for /registrations/{id}_delete for http method type DELETE in rest-assured framework

RoostTestHash=2c75b43156


*/

// ********RoostGPT********
package com.houari.RoostTest;

import io.restassured.RestAssured;
import io.restassured.response.Response;
import org.junit.Before;
import org.junit.jupiter.api.Test;

import java.util.ArrayList;
import java.util.List;
import java.util.Map;

import static io.restassured.RestAssured.given;
import static org.hamcrest.MatcherAssert.assertThat;
import static org.hamcrest.Matchers.instanceOf;
import static org.hamcrest.Matchers.matchesPattern;

public class RegistrationsIdDeleteTest {

    private List<Map<String, String>> envList = new ArrayList<>();

    @Before
    public void setUp() {
        TestDataLoader dataLoader = new TestDataLoader();
        envList = dataLoader.loadData("src/test/java/com/houari/RoostTest/registrations_idDeleteTest.csv");
    }

    @Test
    public void registrationsIdDelete_Test() {
        setUp();
        for (Map<String, String> map : envList) {
            RestAssured.baseURI = map.get("BASE_URL");

            Response response = given()
                    .pathParam("id", map.get("id") != null ? map.get("id") : "")
                    .header("Token", map.get("API_KEY"))
                    .when()
                    .delete("/registrations/{id}")
                    .then()
                    .extract().response();

            switch (response.statusCode()) {
                case 200:
                    System.out.println("Description: successful operation");
                    break;
                case 400:
                    validateErrorResponse(response);
                    break;
                case 401:
                    validateUnprocessableEntityResponse(response);
                    break;
                default:
                    validateNotFoundResponse(response);
                    break;
            }
        }
    }

    private void validateErrorResponse(Response response) {
        System.out.println("Description: Bad Request");
        validateCommonResponseFields(response);
    }

    private void validateNotFoundResponse(Response response) {
        System.out.println("Description: Not Found");
        validateCommonResponseFields(response);
    }

    private void validateUnprocessableEntityResponse(Response response) {
        System.out.println("Description: Unprocessable Entity");
        validateCommonResponseFields(response);
    }

    private void validateCommonResponseFields(Response response) {
        if (response.jsonPath().get("error") != null) {
            assertThat(response.jsonPath().get("error"), instanceOf(String.class));
            assertThat(response.jsonPath().getString("error"), matchesPattern("^validation/.*$"));
        }

        if (response.jsonPath().get("description") != null) {
            assertThat(response.jsonPath().get("description"), instanceOf(String.class));
        }

        if (response.jsonPath().get("value") != null) {
            assertThat(response.jsonPath().get("value"), instanceOf(Object.class));
        }

        if (response.jsonPath().get("field") != null) {
            assertThat(response.jsonPath().get("field"), instanceOf(String.class));
        }

        if (response.jsonPath().get("schema_field") != null) {
            assertThat(response.jsonPath().get("schema_field"), instanceOf(String.class));
        }
    }
}

# ********RoostGPT********

# Test generated by RoostGPT for test advancedReportTest using AI Type  and AI Model 
# 
# Test generated for /kafka/v3/clusters/{cluster_id}/topics_get for http method type GET in artillery framework
# RoostTestHash=5a89adf6e3
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.nmnmn_API_HOST }}"
  plugins:
    expect: {}
  phases:
    - duration: 60
      arrivalRate: 5
  payload:
    path: "kafka_v3_clusters_cluster_id_topics_get_others.csv"
    fields:
      - "cluster_id"
    skipHeader: true

scenarios:
  - name: "List Kafka Topics"
    flow:
      - log: "Starting request to list Kafka topics"
      - get:
          url: "/kafka/v3/clusters/{{ cluster_id }}/topics"
          headers:
            Authorization: "{{ $processEnvironment.nmnmn_AUTH_TOKEN }}"
          expect:
            - statusCode: 200
            - contentType: "application/json"
      - think: 2

      - log: "Testing with invalid cluster ID"
      - get:
          url: "/kafka/v3/clusters/invalid_cluster_id/topics"
          headers:
            Authorization: "{{ $processEnvironment.nmnmn_AUTH_TOKEN }}"
          expect:
            - statusCode: 404
      - think: 1

      - log: "Testing without authorization"
      - get:
          url: "/kafka/v3/clusters/{{ cluster_id }}/topics"
          expect:
            - statusCode: 401
      - think: 1

      - log: "Testing with invalid authorization"
      - get:
          url: "/kafka/v3/clusters/{{ cluster_id }}/topics"
          headers:
            Authorization: "Invalid_Token"
          expect:
            - statusCode: 401
      - think: 1

      - log: "Testing with incorrect HTTP method"
      - post:
          url: "/kafka/v3/clusters/{{ cluster_id }}/topics"
          headers:
            Authorization: "{{ $processEnvironment.nmnmn_AUTH_TOKEN }}"
          expect:
            - statusCode: 405
      - think: 1

# ********RoostGPT********

# Test generated by RoostGPT for test advancedReportTest using AI Type  and AI Model 
# 
# Test generated for /kafka/v3/clusters/{cluster_id}/topics_post for http method type POST in artillery framework
# RoostTestHash=783e9d2ab5
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.mnmn_API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "kafka_v3_clusters_cluster_id_topics_post_others.csv"
    skipHeader: true
  phases:
    - duration: 60
      arrivalRate: 5
      name: "Create Topic Test"

scenarios:
  - name: "Create Kafka Topic"
    flow:
      - log: "Starting Create Topic request"
      - post:
          url: "/kafka/v3/clusters/{{ clusterId }}/topics"
          headers:
            Authorization: "{{ $processEnvironment.mnmn_AUTH_TOKEN }}"
            Content-Type: "application/json"
          json:
            topic_name: "{{ topicName }}"
            partitions_count: "{{ partitionsCount }}"
            replication_factor: "{{ replicationFactor }}"
            configs:
              - name: "{{ configName }}"
                value: "{{ configValue }}"
            validate_only: "{{ validateOnly }}"
          expect:
            - statusCode: 201
            - contentType: json
            - hasProperty: topic_name
      - think: 2

      - log: "Starting Create Topic request with validation only"
      - post:
          url: "/kafka/v3/clusters/{{ clusterId }}/topics"
          headers:
            Authorization: "{{ $processEnvironment.mnmn_AUTH_TOKEN }}"
            Content-Type: "application/json"
          json:
            topic_name: "{{ topicName }}"
            partitions_count: "{{ partitionsCount }}"
            replication_factor: "{{ replicationFactor }}"
            validate_only: true
          expect:
            - statusCode: 200
            - contentType: json
      - think: 2

      - log: "Starting Create Topic request with invalid cluster ID"
      - post:
          url: "/kafka/v3/clusters/invalid-cluster-id/topics"
          headers:
            Authorization: "{{ $processEnvironment.mnmn_AUTH_TOKEN }}"
            Content-Type: "application/json"
          json:
            topic_name: "{{ topicName }}"
            partitions_count: "{{ partitionsCount }}"
            replication_factor: "{{ replicationFactor }}"
          expect:
            - statusCode: 404
      - think: 2

      - log: "Starting Create Topic request with invalid payload"
      - post:
          url: "/kafka/v3/clusters/{{ clusterId }}/topics"
          headers:
            Authorization: "{{ $processEnvironment.mnmn_AUTH_TOKEN }}"
            Content-Type: "application/json"
          json:
            invalid_field: "invalid_value"
          expect:
            - statusCode: 400
      - think: 2

      - log: "Starting Create Topic request without authorization"
      - post:
          url: "/kafka/v3/clusters/{{ clusterId }}/topics"
          json:
            topic_name: "{{ topicName }}"
            partitions_count: "{{ partitionsCount }}"
            replication_factor: "{{ replicationFactor }}"
          expect:
            - statusCode: 401
      - think: 2

# ********RoostGPT********

# Test generated by RoostGPT for test artilleryTest using AI Type  and AI Model 
# 
# ROOST_METHOD_HASH=9adbc3cb1f
# ROOST_METHOD_SIG_HASH=b70f07ae36
# 
#    ########## Scenario ##########  
# 
# {
#   feature: 'Feature: Check broadband service availability and selection',
#   background: 'Background: \n' +
#     '    Given a customer base where each customer has a unique subscriber ID',
#   rule: null,
#   scenario: {
#     title: 'Scenario: Selecting a broadband package based on availability',
#     steps: 'Given "John Doe" received the broadband availability report\n' +
#       'And he finds that "BL_1000MB" broadband speed is available at his address\n' +
#       'When John selects the "BL_1000MB" package\n' +
#       'Then the system should validate the selection\n' +
#       "And confirm the broadband package for John's address",
#     examples: ''
#   }
# }
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}/configuration"
  plugins:
    expect:
      - statusCode: 200
        body: 
          control: 
            type: string
            enum: [S, T, E]
          configuration:
            configuration: 
              action: 
                type: string
                enum: [HGW_WIFI_ENABLE, HGW_WIFI_DISABLE, HGW_WIFI_SET_PASSWD, HGW_WIFI_CONFIGURATION, GPON_ONT_RESET]
              customer: 
                customer: 
                  subscriberId: string
              id: string
              adminStatus: string
              startDate: string
              endDate: string
              state: string
              diagnostic: 
                diagnostic: 
                  code: string
                  messages: 
                    message: 
                      - errorCode: string
                        description: string
                        criticity: string

scenarios:
  - name: "Scenario: Selecting a broadband package based on availability"
    flow:
      - post:
          url: "{{ $processEnvironment.API_HOST }}/configuration"
          json:
            configuration:
              action:
                type: HGW_WIFI_ENABLE
                parameters:
                  frequencyBand: 2.4GHz
              customer:
                subscriberId: SVTAL001
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 200
              json:
                control:
                  type: S
                configuration:
                  configuration:
                    action:
                      type: HGW_WIFI_ENABLE
                    customer:
                      customer:
                        subscriberId: SVTAL001

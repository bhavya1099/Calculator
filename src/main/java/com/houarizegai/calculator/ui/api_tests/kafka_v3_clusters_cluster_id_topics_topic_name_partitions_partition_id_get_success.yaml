# ********RoostGPT********

# Test generated by RoostGPT for test artilleryTest using AI Type  and AI Model 
# 
# Test generated for /kafka/v3/clusters/{cluster_id}/topics/{topic_name}/partitions/{partition_id}_get for http method type GET in artillery framework
# RoostTestHash=0ecd20dc17
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.uiop_API_HOST }}"
  plugins:
    expect: {}

scenarios:
  - name: Test API Requests
    flow:
      - log: "Send GET request to /kafka/v3/clusters/{cluster_id}/topics/{topic_name}/partitions/{partition_id}"
      - get:
          url: "/kafka/v3/clusters/{cluster_id}/topics/{topic_name}/partitions/{partition_id}"
          headers:
            Authorization: "{{$processEnvironment.uiop_AUTH_TOKEN}}"
      - think: 1
      - log: "Send POST request to /kafka/v3/clusters/{cluster_id}/topics/{topic_name}/partitions/{partition_id}"
      - post:
          url: "/kafka/v3/clusters/{cluster_id}/topics/{topic_name}/partitions/{partition_id}"
          headers:
            Authorization: "{{$processEnvironment.uiop_AUTH_TOKEN}}"
          payload:
            # Define payload here
      - think: 1
      - log: "Send PUT request to /kafka/v3/clusters/{cluster_id}/topics/{topic_name}/partitions/{partition_id}"
      - put:
          url: "/kafka/v3/clusters/{cluster_id}/topics/{topic_name}/partitions/{partition_id}"
          headers:
            Authorization: "{{$processEnvironment.uiop_AUTH_TOKEN}}"
          payload:
            # Define payload here
      - think: 1
      - log: "Send PATCH request to /kafka/v3/clusters/{cluster_id}/topics/{topic_name}/partitions/{partition_id}"
      - patch:
          url: "/kafka/v3/clusters/{cluster_id}/topics/{topic_name}/partitions/{partition_id}"
          headers:
            Authorization: "{{$processEnvironment.uiop_AUTH_TOKEN}}"
          payload:
            # Define payload here
      - think: 1
      - log: "Send DELETE request to /kafka/v3/clusters/{cluster_id}/topics/{topic_name}/partitions/{partition_id}"
      - delete:
          url: "/kafka/v3/clusters/{cluster_id}/topics/{topic_name}/partitions/{partition_id}"
          headers:
            Authorization: "{{$processEnvironment.uiop_AUTH_TOKEN}}"
      - think: 1

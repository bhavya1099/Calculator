# ********RoostGPT********

# Test generated by RoostGPT for test artilleryTest using AI Type  and AI Model 
# 
# Test generated for /kafka/v3/clusters/{cluster_id}/topics/{topic_name}_delete for http method type DELETE in artillery framework
# RoostTestHash=b9526a5d59
# 
# 

# ********RoostGPT********
config:
  plugins:
    expect: {}
  target: "{{ $processEnvironment.uiop_API_HOST }}"

scenarios:
  - name: Test Success API Responses
    flow:
      - log: 
        - "Sending GET request to /kafka/v3/clusters/{cluster_id}/topics/{topic_name}"
      - get:
          url: "/kafka/v3/clusters/{cluster_id}/topics/{topic_name}"
          headers:
            Authorization: "{{ $processEnvironment.uiop_AUTH_TOKEN }}"
      - think: 2
      - log:
        - "Sending POST request to /kafka/v3/clusters/{cluster_id}/topics/{topic_name}"
      - post:
          url: "/kafka/v3/clusters/{cluster_id}/topics/{topic_name}"
          headers:
            Authorization: "{{ $processEnvironment.uiop_AUTH_TOKEN }}"
          payload:
            # Add payload fields here
      - think: 2
      - log:
        - "Sending PATCH request to /kafka/v3/clusters/{cluster_id}/topics/{topic_name}"
      - patch:
          url: "/kafka/v3/clusters/{cluster_id}/topics/{topic_name}"
          headers:
            Authorization: "{{ $processEnvironment.uiop_AUTH_TOKEN }}"
          payload:
            # Add payload fields here
      - think: 2
      - log:
        - "Sending DELETE request to /kafka/v3/clusters/{cluster_id}/topics/{topic_name}"
      - delete:
          url: "/kafka/v3/clusters/{cluster_id}/topics/{topic_name}"
          headers:
            Authorization: "{{ $processEnvironment.uiop_AUTH_TOKEN }}"
      - think: 2

